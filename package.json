{
    "name": "First_addons",
    "displayName": "First_addons",
    "description": "First addons sample",
    "version": "0.0.0",
    "publisher": "vscode-samples",
    "repository": "https://github.com/OlivierArgentieri/first_vsc_extension",
    "engines": {
        "vscode": "^1.22.0"
    },
    "categories": ["Other"],
    "activationEvents": ["onCommand:extension.command"],
    "main": "./out/extension.js",
    "contributes": {
        "commands": [
            {
                "command": "extension.command",
                "title": "First_Addons"
            }
        ]
    },
    "scripts": {
        "vscode:prepublish": "npm run compile",
        "compile": "tsc -p ./",
        "watch": "tsc -watch -p ./"
    },
    "devDependencies": {
        "@types/node": "^16.6.2",
        "@types/vscode": "^1.59.0",
        "tslint": "^6.1.3",
        "typescript": "^4.3.5"
    }
}